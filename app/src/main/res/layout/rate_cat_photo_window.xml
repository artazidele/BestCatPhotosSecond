<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:padding="16dp">
    <Button
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:backgroundTint="@color/white"
        android:drawableLeft="@drawable/close_button_background"
        android:padding="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:id="@+id/cancel_button"/>
    <ImageView
        android:layout_marginTop="8dp"
        android:layout_width="300dp"
        android:layout_height="400dp"
        android:scaleType="centerCrop"
        android:padding="4dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/cancel_button"
        android:id="@+id/cat_image_view"/>
    <LinearLayout
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/cat_image_view">
        <Button
            android:layout_width="96dp"
            android:id="@+id/rate_negative_button"
            android:layout_height="56dp"
            android:drawableTop="@drawable/thumb_down_icon" />

        <Button
            android:id="@+id/rate_positive_button"
            android:layout_width="96dp"
            android:layout_height="56dp"
            android:drawableTop="@drawable/thumb_up_icon" />
    </LinearLayout>


</androidx.constraintlayout.widget.ConstraintLayout>