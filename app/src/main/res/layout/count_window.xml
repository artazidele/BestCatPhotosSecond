<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/rate_dialog_border">

    <TextView
        android:id="@+id/count_text_view"
        android:layout_width="280dp"
        android:textSize="16sp"
        android:padding="8dp"
        android:textColor="@color/black"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="How much cat photos do You want to find?"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <EditText
        android:textSize="16sp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/count_text_view"
        android:id="@+id/count_edit_text"
        android:layout_width="160dp"
        android:gravity="center"
        android:layout_height="48dp"
        android:layout_marginTop="8dp"
        android:background="@drawable/edit_text_border"
        android:cursorVisible="true"
        android:inputType="number"
        android:padding="4dp" />

    <Button
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/count_edit_text"
        android:id="@+id/choose_count_button"
        android:layout_width="wrap_content"
        android:layout_margin="8dp"
        android:text="Find cats"
        android:layout_height="56dp" />

</androidx.constraintlayout.widget.ConstraintLayout>